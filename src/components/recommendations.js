// Stub recommendations component to avoid 404 during service-worker precache
// Real implementation can be added later.
(function(){
  window.Recommendations = window.Recommendations || {
    init: function(){ /* no-op */ }
  };
})();
