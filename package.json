{
  "name": "tuwa-web-player",
  "version": "2.0.0",
  "description": "Premium distraction-free audio streaming player.",
  "scripts": {
    "dev": "wrangler pages dev _site",
    "deploy": "wrangler pages deploy _site --project-name=tuwa-player",
    "obfuscate": "javascript-obfuscator ./_site --output ./_site --compact true --self-defending true --disable-console-output true --debug-protection true"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.2.2",
    "base64-js": "^1.5.1",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}